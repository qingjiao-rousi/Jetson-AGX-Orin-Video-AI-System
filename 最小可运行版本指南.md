# 从零到一完成最小可运行版本指南

## 目标

先做一个最小可运行版本，不追求一次做满全部能力，只追求：

- 能在开发板环境里跑起来。
- 能拉起 1 路视频。
- 能完成解码、推理、跟踪、输出的主链路。
- 能看到状态、日志和结果。

## 第 0 步：先确认项目边界

### 你现在已经有的

- 分层架构骨架。
- 配置文件骨架。
- Web 状态页骨架。
- 测试骨架。

### 你还不能缺的资源

- Jetson AGX Orin 32G。
- JetPack 6.2.2。
- DeepStream 对应版本。
- YOLOv8 模型资源。
- TensorRT engine 或可用于生成 engine 的 ONNX。
- 自定义 parser `.so` 或至少能临时替代的解析方案。
- 一路可用的视频源。

## 第 1 步：先把环境确认清楚

你需要先确认这些信息：

- JetPack 是否已经正确刷好。
- CUDA、TensorRT、DeepStream 是否安装完整。
- `pyds` 是否可导入。
- GStreamer 是否可用。
- Python 版本是否和当前项目兼容。
- 是否能打开 RTSP / 本地视频流。

如果这里任何一项不确定，先不要往下写业务代码，先把环境验证过。

## 第 2 步：准备最小资源

你至少要准备：

- 1 个视频源，优先本地视频，便于排障。
- 1 个模型文件，优先 YOLOv8n。
- 1 个 TensorRT engine，或至少 1 个 ONNX。
- 1 个检测类别文件或标签列表。
- 1 个 DeepStream 自定义解析库，如果你走自定义 YOLO 输出解析。

如果暂时没有这些资源，可以先用占位配置和 mock 数据把程序框架跑通，但这不算最小可运行版本完成。

## 第 3 步：先跑通“环境级验证”

建议顺序：

1. 验证 `python` 版本和依赖安装。
2. 验证 `pyds` 能否导入。
3. 验证 DeepStream sample 是否能跑。
4. 验证 `gst-launch` 或同等工具能否打开视频流。

这一阶段的目标不是你自己的项目，而是先确认板端运行环境是活的。

## 第 4 步：先做单路最小链路

最小链路建议是：

`一路视频 -> 解码 -> 推理 -> 输出结果`

先不要上来就做：

- 6 路。
- 多线程并行推理。
- WebSocket。
- MQTT。
- Kafka。
- 复杂优化策略。

先把单路跑通，才能避免后面同时排查多个变量。

## 第 5 步：按模块逐个闭环

### 5.1 配置闭环

你要准备：

- `app.yaml`
- `streams.yaml`
- `infer_primary_yolo.txt`
- `streammux.yaml`
- `tracker_iou.yml`

要求：

- 路数先设为 1。
- batch size 先设为 1。
- 分辨率先用常见值。
- 输出先用 JSONL 或日志，不要一开始就接复杂外部系统。

### 5.2 模型闭环

你要准备：

- `yolov8n.pt` 或等价训练权重。
- 导出的 `onnx`。
- 生成的 `engine`。
- 类别名和标签。
- 自定义 parser `.so`。

要求：

- 先 FP16。
- 先别急着 INT8。
- 先别急着多模型串联。

### 5.3 视频源闭环

你要准备：

- 本地视频文件，优先。
- RTSP 流作为第二阶段。

要求：

- 先验证单路文件输入。
- 再验证 RTSP。
- 再扩展到多路。

### 5.4 输出闭环

你要准备：

- JSONL 输出路径。
- 日志目录。
- 如果要推流，再补 RTMP 配置。

要求：

- 先确认结果能写出来。
- 再确认页面能看见。
- 再确认外部系统能接收。

## 第 6 步：把你当前代码按顺序补齐

建议顺序：

1. 先确认配置层完全正确。
2. 再确认 pipeline builder 能生成完整蓝图。
3. 再确认 manager 能处理 bus 状态。
4. 再确认 probe 能拿到真实 payload。
5. 再接 `NvDsBatchMeta`。
6. 再接输出写入和 Web 展示。

## 第 7 步：你需要我配合时，优先提供哪些资源

按优先级排序：

1. Jetson 上的 DeepStream / JetPack 安装状态。
2. 你要用的视频源地址或本地文件。
3. 模型文件。
4. engine 文件。
5. 标签文件。
6. 自定义 parser `.so`。
7. 你希望输出到哪里：JSONL、RTMP、MQTT、Web。

## 第 8 步：最小可运行版本的验收标准

达到下面这些，才算最小可运行版本：

- 程序能在开发板启动。
- 一路视频能完整进入 pipeline。
- 推理能出结果。
- 结果能落 JSONL 或日志。
- Web 页面能看到状态。
- bus 错误能被记录。

## 第 9 步：后续再升级什么

等最小版本稳定后，再逐步做：

- 1 路 -> 4 路 -> 6 路。
- FP16 -> INT8。
- 单输出 -> 多输出。
- 简单 tracker -> 更强 tracker。
- 单机调试 -> 稳定化部署。


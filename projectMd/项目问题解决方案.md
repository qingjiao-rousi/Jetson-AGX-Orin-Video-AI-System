# 项目问题解决方案文档

## #2 多路多模型推理

**现状**：只有单模型 YOLOv8n（`nvinfer` 的 `primary-infer`），无双模型或 SGIE。

**方案**：在 `builder.py` 里加 SGIE 节点和链路。

### 模型关系说明

```
PGIE (Primary GIE) — 主检测           SGIE (Secondary GIE) — 次级分类
───────────────────────────           ───────────────────────────────
YOLOv8n: 全图检测80类                  领域模型: 只推理 PGIE 输出的 ROI 区域
"在哪里、是什么(粗略)"                    "具体是什么(精细)"
                                    process-mode=2: ROI推理,不减全图
```

**两者不冲突**：PGIE 做粗检测，SGIE 在检测到的目标框内做细分类。不同类别的 ROI 可路由到不同 SGIE 模型。

### 改动清单

**文件 1**：[src/app/infrastructure/pipeline/builder.py](src/app/infrastructure/pipeline/builder.py)

在 `_build_nodes()` 中 `tracker` 节点之后加入：

```python
# 次级推理（可选 — flags["required"]=False）
PipelineNodeSpec(
    "secondary-classifier",
    "nvinfer",
    "classify",
    {
        "config-file-path": str(ds.sgie_config_path),
        "model-engine-file": str(ds.sgie_engine_path),
        "process-mode": 2,          # ★ 只推理 ROI 区域，不对全图
        "operate-on-gie-id": 1,     # 在 PGIE(gie-id=1) 的结果上推理
        "operate-on-class-ids": 0,  # 只对 class_id=0(person) 的 ROI 推理
    },
    {"required": False, "live": True, "supports_probe": True, "hardware_accelerated": True},
),
```

在 `_build_links()` 中，将 `tracker → osd` 改为 `tracker → secondary-classifier → osd`。

**文件 2**：[src/app/settings.py](src/app/settings.py) — `DeepStreamSettings` 加字段：

```python
sgie_engine_path: Path = Path("models/resnet18.engine")
sgie_config_path: Path = Path("configs/deepstream/sgie_classifier.txt")
```

**文件 3**：新增 `configs/deepstream/sgie_classifier.txt`

```ini
[property]
gpu-id=0
model-engine-file=models/resnet18.engine
labelfile-path=models/vehicle_labels.txt
batch-size=6
network-mode=2
num-detected-classes=4
interval=0
gie-unique-id=2
process-mode=2
operate-on-gie-id=1
operate-on-class-ids=2
```

**面试表达**：

> "系统支持 PGIE + SGIE 级联推理架构。PGIE 用 YOLOv8n 做全图目标检测，SGIE 在检测到的 ROI 区域上运行分类或属性模型。通过 `process-mode=2` 让 SGIE 只推理目标区域，不扫描全图，计算量比独立跑两个全图模型小一个数量级。"

## #3 模型数据来源

**现状**：配置指向 `models/yolov8n.engine`，但文件不存在。

### 获取流程

```
下载 PyTorch 权重(.pt)  →  导出 ONNX(.onnx)  →  构建 TensorRT Engine(.engine)
─────────────              ──────────────         ──────────────
任何机器                   任何机器(有GPU更快)     必须在 Jetson 上执行
wget from GitHub           ultralytics.export()    trtexec
```

### 具体步骤

```bash
# Step 1: 下载 YOLOv8n PyTorch 权重 (任何机器)
wget https://github.com/ultralytics/assets/releases/download/v8.2.0/yolov8n.pt \
    -O models/yolov8n.pt

# Step 2: 导出 ONNX (任何机器, pip install ultralytics)
python3 -c "
from ultralytics import YOLO
model = YOLO('models/yolov8n.pt')
model.export(format='onnx', opset=17, imgsz=640, dynamic=False, simplify=True)
"

# Step 3: 构建 TensorRT Engine (必须在 Jetson AGX Orin 上)
/usr/src/tensorrt/bin/trtexec \
    --onnx=models/yolov8n.onnx \
    --saveEngine=models/yolov8n.engine \
    --fp16 \
    --workspace=2048 \
    --optShapes=input:6x3x640x640 \
    --minShapes=input:1x3x640x640 \
    --maxShapes=input:6x3x640x640

# Step 4: 创建 labels.txt (COCO 80类)
# 类别列表见 scripts/download_models.sh
```

### 开源预训练模型来源


| 模型             | 用途              | 来源                                     |
| ---------------- | ----------------- | ---------------------------------------- |
| YOLOv8n/s/m/l/x  | 80类检测 (COCO)   | `github.com/ultralytics/assets/releases` |
| YOLOv11n/s/m/l/x | 80类检测 (COCO)   | 同上                                     |
| RetinaFace       | 人脸检测+关键点   | `github.com/biubug6/Pytorch_Retinaface`  |
| SCRFD            | 人脸检测 (更轻量) | `github.com/deepinsight/insightface`     |
| HyperLPR         | 中文车牌识别      | `github.com/szad670401/HyperLPR`         |
| YOLOv8-Face      | YOLO 架构人脸检测 | 搜`yolov8-face`                          |
| YOLOv8-Hardhat   | 安全帽检测        | 搜`yolov8-hardhat`                       |

### 模型对比切换规则


| 对比场景                 | 只改路径 | 需额外改动                             |
| ------------------------ | :------: | -------------------------------------- |
| YOLOv8n → YOLOv8s/m/l/x |    ✅    | engine 路径                            |
| YOLOv8n FP16 → INT8     |    ✅    | engine 路径                            |
| YOLOv8n → YOLOv11n      |    ✅    | parser 大概率通用                      |
| YOLOv8n → YOLOv5n       |    ❌    | v5 anchor-based，parser 需重写         |
| YOLO → EfficientDet     |    ❌    | parser .so + labels + 输入尺寸         |
| 同架构，不同类数         |    ❌    | engine + labels + num-detected-classes |

## #4 丢帧保持最新帧

**现状**：`FpsController.observe()` 和 `BackpressureController.observe()` 只记录不决策。

### 方案

**三层设计**：数据采集 → 决策逻辑 → 执行

```
每帧 → observe(result)
         │
         ├─ 滑动窗口统计 GPU 利用率 + 队列深度
         │
         ├─ 决策: GPU>85% 或 queue>80% → drop_rate↑
         │        GPU<60% 且 queue<40% → drop_rate↓
         │
         └─ 返回 bool: True=丢帧 / False=正常处理
              │
              ▼
         orchestrator.on_frame_result:
           if fps_controller.observe(result) == True:
               return  # 跳过本帧
```

### 改动清单

**文件 1**：[src/app/optimization/fps_controller.py](src/app/optimization/fps_controller.py)

```python
# observe() 核心逻辑
def observe(self, result) -> bool:
    gpu_pct = self._gpu_monitor.gpu_util()       # 从 GPU 监控器读
    queue_pct = self._backpressure.queue_depth_ratio()  # 从背压控制器读

    # 决策: 阶梯式调整 drop_rate
    if gpu_pct > 85.0 or queue_pct > 0.80:
        self._drop_rate = min(0.90, self._drop_rate + 0.15)
    elif gpu_pct < 60.0 and queue_pct < 0.40:
        self._drop_rate = max(0.0, self._drop_rate - 0.05)

    return random.random() < self._drop_rate  # 概率丢帧, 避免同步抖动
```

需要新增属性：`_gpu_monitor` (绑定 GPU 监控器)、`_backpressure` (绑定背压控制器)、`_drop_rate` (当前丢帧概率)。

**文件 2**：[src/app/optimization/backpressure_controller.py](src/app/optimization/backpressure_controller.py)

```python
# 生产-消费时间戳对账
def observe(self, result):
    self._produce_ts.append(time.monotonic())      # 记录生产时间

def mark_consumed(self):
    self._consume_ts.append(time.monotonic())      # 记录消费时间

def queue_depth_ratio(self) -> float:
    pending = len(self._produce_ts) - len(self._consume_ts)
    return min(1.0, pending / self._max_queue_size)
```

需要新增属性：`_produce_ts` / `_consume_ts` (deque)、`_max_queue_size`。

**文件 3**：[src/app/application/orchestrator.py](src/app/application/orchestrator.py) — `on_frame_result()` 改为：

```python
def on_frame_result(self, result):
    if self.fps_controller.observe(result):  # True = 丢帧
        return
    parsed = self.meta_parser.parse(result)
    self.backpressure_controller.observe(parsed)
    self.json_writer.write(parsed)
    self.backpressure_controller.mark_consumed()
```

**文件 4**：[src/app/bootstrap.py](src/app/bootstrap.py) — 加绑定：

```python
fps_controller.bind_gpu_monitor(gpu_monitor)
fps_controller.bind_backpressure(backpressure_controller)
```

**文件 5**：[configs/app/app.yaml](configs/app/app.yaml) — `optimization` 段扩展：

```yaml
optimization:
  max_queue_size: 32
  fps_min: 5.0
  fps_max: 30.0
  enable_fps_control: true
  enable_backpressure: true
  gpu_high_threshold: 85.0        # ← 新增
  gpu_low_threshold: 60.0         # ← 新增
  queue_high_ratio: 0.80          # ← 新增
  queue_low_ratio: 0.40           # ← 新增
```

### 面试表达

> "丢帧控制采用阶梯式概率丢弃策略——根据 GPU 利用率和内部队列深度动态调整 drop_rate，每次调整 0.15（上升）或 0.05（恢复），用概率丢弃而非固定跳帧模式避免与摄像头帧率产生同步共振。drop_rate 上限 90% 防止 pipeline 完全停滞。"

## #5 多线程解耦

**现状**：主线程跑全部（Pipeline + 解析 + 输出），仅 Web 有独立 daemon 线程。

### 方案

分阶段推进：

**阶段 A — 上板前做（改动最小）**：JSON 写出异步化

```
主线程: Pipeline → Probe → meta_parser → 放入 Queue
线程 2: 从 Queue 取 → json_writer.write() → backpressure.mark_consumed()
```

用 `queue.Queue(maxsize=200)`，队列满时 `put()` 阻塞 = 自然背压。

改动点：

- [bootstrap.py](src/app/bootstrap.py)：创建 Queue + 启动消费者线程
- [orchestrator.py](src/app/application/orchestrator.py)：`on_frame_result()` push 到 Queue 而非直接 write

**阶段 B — 上板后做**：GPU 监控独立线程

```
线程 3: tegrastats 轮询 (1s 周期) → 写入共享变量 (带 Lock)
        FpsController 从共享变量读，不参与轮询
```

改动点：`gpu_monitor.py` 加 `start()`/`stop()` + 后台轮询。

**阶段 C — 远期**：进程拆分（推理 vs Web）

```
进程 1 (绑定 GPU): Pipeline → C++ Probe → 共享内存 Ring Buffer
进程 2 (Web):       读 Ring Buffer → WebSocket 推送
进程 3 (输出):      读 Ring Buffer → JSONL/MQTT/Kafka
```

这是 AGX.md 模块三的定位，用 C++ lock-free ring buffer 做跨进程 IPC。

### 面试表达

> "当前版本 JSON 写出已异步化——主推理链路只负责解析和入队，独立线程消费写入，队列满时自动背压丢帧。GPU 监控在独立线程轮询不会阻塞推理。后续进程拆分方案已在架构文档中完成设计，用 C++ 共享内存 lock-free ring buffer 做零拷贝跨进程通信。"

## #6 异常处理加固

**现状**：入口层和配置校验层完善，但 `meta_parser.py` 零异常处理，两个 controller 零异常处理。

### 按优先级修复

**P0 — `meta_parser.py`**：

`parse()` 方法整体包 try/except，解析失败返回空 `FrameResult` + 打 error 日志：

```python
def parse(self, raw_meta: object) -> FrameResult:
    try:
        payload = self._normalize(raw_meta)
        # ... 原有解析逻辑
        return FrameResult(...)
    except Exception:
        logging.exception("meta_parser failed to parse raw_meta type=%s", type(raw_meta).__name__)
        return FrameResult(
            stream_id="unknown",
            frame_id=0,
            timestamp=datetime.now(UTC),
            detections=[],
            tracks=[],
            extra={"parse_error": True},
        )
```

**P0 — `json_writer.py`**：

`write()` 方法包 try/except around `json.dumps()`：

```python
def write(self, result: object) -> None:
    try:
        payload = asdict(result)
        line = json.dumps(payload, ensure_ascii=False, default=str)
    except Exception:
        logging.warning("json_writer serialization failed for %s", type(result).__name__)
        return
    with self._lock:
        self._file.write(line + "\n")
        self._file.flush()
```

**P1 — `builder.py` probe 回调**：

`_cast_meta()` 失败时加字典 fallback（直接读字段，不依赖 pyds.cast）：

```python
def _cast_meta(self, value: object) -> object:
    # 已有 pyds cast 逻辑...
    # fallback: 如果 cast 失败，直接返回 dict-like wrapper
    return value  # 让 _safe_get 兜底处理
```

**P1 — `orchestrator.py`**：

`handle_error()` 改抛异常为有序重启：

```python
def handle_error(self, message: str) -> None:
    logging.error("pipeline error: %s, attempting restart in 3s", message)
    self.pipeline_manager.set_error(message)
    time.sleep(3)
    try:
        self.pipeline_manager.restart()
    except Exception:
        logging.critical("pipeline restart failed, shutting down")
        self._stop_event.set()
```

**P2 — `fps_controller.py` / `backpressure_controller.py`**：

`observe()` 包 try，异常时返回安全默认值：

```python
def observe(self, result: object) -> bool:
    try:
        # 正常决策逻辑...
    except Exception:
        logging.warning("fps_controller observe failed", exc_info=True)
        return False  # 安全默认: 不丢帧
```

## #7 内存对齐

**现状**：Python 层没有内存对齐，也不应该做。AGX.md 的 C++ 模块设计已覆盖。

### 为什么 Python 层不做

- Python 对象全是堆分配 + GC 管理，`dataclass`/`dict` 的内存布局由 CPython 解释器完全控制
- 在 Python 里加 `__slots__` 或手动 padding 对 GPU DMA buffer 的读写毫无影响
- 热路径的性能瓶颈不在 Python 的数据布局，而在 Python↔C 的跨边界开销

### 正确做法（C++ 层）

AGX.md 四个 C++ 模块中已设计，落点：


| 模块                | 文件                                    | 对齐设计                                                                                           |
| ------------------- | --------------------------------------- | -------------------------------------------------------------------------------------------------- |
| 模块二: Probe 回调  | `custom_libs/probe_handler/`            | 不涉及                                                                                             |
| 模块三: Ring Buffer | `custom_libs/shm_bus/shm_ring_buffer.h` | `alignas(64) DetectionResult` (64B = 1 cache line) + `alignas(64) atomic<size_t>` 四个指针各占一行 |
| 模块四: CUDA Kernel | `custom_libs/cuda_kernels/`             | CUDA kernel launch 时保证 coalesced memory access，block/grid 维度取 32 的倍数                     |

Python 端通过 `ctypes.Structure._fields_` 精确映射 C++ POD 内存布局做零拷贝读取，字段顺序和 padding 与 C++ 一致即可。

## 优先级总览

```
上板前必须完成:
  📦 #3 模型文件获取        — 没有 engine 什么都跑不起来
  🗑️ #4 丢帧逻辑实现        — 6路没有背压保护可能 OOM 或卡死
  🛡️ #6 meta_parser + json_writer 异常处理  — 一帧坏数据不能崩全链路

上板跑通后优化:
  🔀 #2 多模型 SGIE 架构     — 单模型跑稳后再扩展
  🧵 #5 异步消费队列         — 模型对比跑出瓶颈后再做
  🛡️ #6 其余异常处理点       — 随稳定性测试逐步加固

C++ 模块落地时自然解决:
  📐 #7 内存对齐             — 跟着 AGX.md 模块二/三/四一起实现
  🎨 #1 RTSP→YUV            — DeepStream 已覆盖，无需自定义

不需要改动:
  ✅ #1 RTSP→YUV            — DeepStream 硬件加速链路完整
```
